def main():
    ranges_string = ''
    ranges_list = string_to_list(ranges_string)
    ranges_two_d_list = list_to_two_d_list(ranges_list)

def string_to_list(ranges_string):
    #TO DO: Convert initial string to an array to separate the ranges

def list_to_two_d_list(ranges_list):
    #TO DO: Turn each range into a two dimensional list to easily access the min and max later

def check_validity(ranges_two_d_list):
    #TO DO: Add logic to loop through and check each number in each range against the challenge criteria

main()